---
task_id: REPLACE_WITH_STABLE_ID
base_sha: REPLACE_WITH_GIT_HEAD
worktree: REPLACE_WITH_ABSOLUTE_PATH
branch: REPLACE_WITH_BRANCH
owned_paths:
  - REPLACE_WITH_PATH
integrator: codex-integrator
---

# Change contract

- Acceptance criteria:
- Non-goals:
- Expected tests/gates:

# Return packet

- `git diff --stat` and changed paths:
- Tests run with exit status:
- Known risks / unverified behavior:
- Stop instead of rebasing or resolving a conflict outside owned paths.
