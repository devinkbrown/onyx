# Roadmap source ledger

Use this ledger before selecting a roadmap slice. The roadmap sets direction; source and tests establish shipped status.

| Slice | Roadmap claim | Verified source and test evidence | Status |
| --- | --- | --- | --- |
| Era 1 A1 hybrid search honesty | Hybrid search docs + UI “on this device” copy | `docs/search-and-history.md` matches MessageSearch mode titles/provenance (`This device` / `This server`), hybrid default, no cloud-AI claim; UI already labels modes/results “on this device”; tests in `MessageSearch.test.tsx` | closed 2026-07-19 |
| Reader reviewed-anchor handoff | Carry reviewed anchors into richer cross-room handoffs | `&` rooms eligible via `targetLooksLikeChannel`; moment `?join=` accepts `#&`; reader memory surfaces peer-reviewed anchors via `peerReviewedAnchors` + exact-id travel | closed 2026-07-19 |
| Cold return Home catch-up | Home paints catch-up from vault before network | `catchUpMemory` owner-scoped snapshot; HomeView cold path (`data-catchup-source=memory`) + vault recap seeds; live handoff once rooms hydrate; tests in `catchUpMemory.test.ts` + `HomeView.coldCatchUp.test.tsx` | closed 2026-07-19 |
| Background consolidation | Consolidate to signature presets with bounded ambient rendering | All canvas variants route through `composeSignature` (`src/backgrounds/variants/*`); catalogue groups 24 selectable ids into five signature families + `scene` (`catalogue.ts` `SIGNATURE_FAMILIES`); frost/lapis-gradient/obsidian holdouts closed; SceneShell static grain+vignette; Animated/Still/Off + reduced-motion still engine-owned; tests in `backgrounds.test.tsx` / `layers.test.ts` / `SceneShell.test.tsx` (`9629f2d`) | closed 2026-07-21 |
| Dense nicklist browser evidence | Prove dense surface reflow in the real shell | `tests/e2e/member-list-dense-reflow.spec.ts` seeds a 48-member roster through the production runtime store (real AppShell, not `setContent`); asserts mobile drawer + desktop column containment, scroll-to-tail, focus reachability (`e65675a`); static `member-list-reflow.spec.ts` remains a CSS-only companion | closed 2026-07-21 |
| Era 2 B1 session TOKEN/MTOKEN multi-device polish | Capture, persist, refresh, resume TOKEN/MTOKEN; suppress JOIN storm under session-sync; fail-closed bearers | Constructor + live-client `isValidSessionCredential` sanitize; store identity guards on replaced-client callbacks; reconnect rejoin skips when `onyx/session-sync` active; tests in `client.test.ts` + `store.sessionRoster.test.ts` | closed 2026-07-19 |
| Era 2 B4 ACCESS roles UI | ACCESS + PROP room settings (roles, encryption-policy, history-policy) | Encryption + history already in ChannelSettings (PROP EPHEMERAL / encryption-policy); ACCESS list manager in ChannelSettings (op-gated LIST/ADD/DELETE, 801–805 fold, PART clears list, 801 map-cap); pure helpers `src/lib/irc/channelAccess.ts`; tests `channelAccess.test.ts`, `store.access.test.ts`, ChannelSettings ACCESS suite + channel-moderation PROP suite | closed 2026-07-19 |
| Era 2 B3 EDIT/REDACT/react/reply polish | Reply/edit mutual exclusivity + target-scoped reply tags + Escape cancel; case-insensitive reaction toggle; outbound EDIT/REDACT; pins ribbon chip | `messageContext.ts`; `toggleReaction.ts`; store exclusive setReplyingTo/setComposerEditingMessage + activeReplyForTarget on send; addLocalReaction/removeReaction pure helpers; Composer banners/Escape/focus/typing-stop; PresenceRibbon Place pins chip; tests in `messageContext.test.ts`, `toggleReaction.test.ts`, `store.composerContext.test.ts`, `store.reply.test.ts`, `store.reactions.test.ts`, `store.editRedact.test.ts`, `Composer.test.tsx`, `PresenceRibbon.overflow.test.tsx` | partial 2026-07-19 — react/reply/EDIT/REDACT + pins bar shipped; typing idle gate + multiline already solid; deeper multiline send UX polish optional |
| Era 2 B2 Full MEDIA UI polish | Join muted option, CQ soft actions, Privacy sheet; EVENT MEDIA roster already live | R1–R6: `muteOnJoin` in `voiceSettingsPersistence` + VoiceSettings + `joinVoiceChannel`/`acceptDmCall`; CQ hysteresis `connectionQualityAction.ts` → VoiceBar turn-off-camera prompt; Privacy sheet via security chip + `callPrivacy.ts`; **R3** bandwidth ladder toast — `bandwidthLadderFeedback.ts` wired in `VoiceBar.tsx`; **R5** codec-failure toast — `codecFailure.ts` wired in `useCadenceMedia.ts` (`onError` / `onDecodeError`); tests in `bandwidthLadderFeedback.test.ts`, `codecFailure.test.ts`, `connectionQualityAction.test.ts`, `callPrivacy.test.ts`, `voiceSettingsPersistence.test.ts`, `voice.test.tsx`, `VoiceSettings.test.tsx`, `useCadenceMedia.test.ts` | closed 2026-07-21 — R1–R6 shipped (`d415302` R3/R5) |
| Vault hydrate / time-travel polish | Cold resume before JOIN + dual-path `?at=` paint; E2EE never plaintext IDB | Owner-scoped `vaultResumeMemory`; `vaultSync` cold paint; `hydrateHistory` activate + chrono merge; `travelTo` vault-first then CHATHISTORY; `deserializeMessage` strips OMIT_AT_REST; tests `vaultSync`/`vaultResumeMemory`/`store.hydrateHistory`/`store.timetravel`/`historyVault` | closed 2026-07-19 |
| MESSAGE_V2 mIRC / announce color admit | Colored PRIVMSG (mIRC C0 format codes) must admit under active MESSAGE_V2 authoring; announce commit cards keep multi-line colour | **Server (HEAD `d158489` + follow-ons):** body text uses `validTextField` / `isIrcFormatControl` in `message_relay.zig` — allows `\x02/\x03/\x04/\x0f/\x11/\x16/\x1d/\x1e/\x1f`, still rejects CR/LF/NUL/SOH/DEL; test `semantic validation allows mIRC formatting controls in message text only`. Channel + direct PRIVMSG fail-closed durable admit when authoring active (`a87372b`, `54b9fb4`). Announce bot multi-line colour cards (`d047f0c`); client `4812fbf` verifies colours after admit. | closed 2026-07-19 |
| Roster NAMES mid-burst / mesh collapse | Interleaved or late 353 must never wipe a full nicklist to a partial; JOIN must not re-arm REPLACE mid-burst | **Client (HEAD `9cf2a50`):** `_beginNamesBurst` refuses re-arm while phase is `expect`/`appending` (`store.ts`); self-JOIN only arms, does not send a second NAMES; first 353 of a client-initiated burst REPLACES, all other 353 APPEND. Tests: `store.roster.test.ts` (mid-burst re-arm, late partial, settled focus), `store.channel.test.ts` (self-JOIN arms without second NAMES). **Server (`a87372b`):** `names_reply.writeNamReplyLines` streams multi-buffer 353; bad/overlong member tokens are **skipped** rather than aborting to a bare 366. | closed 2026-07-19 |
| Helix Orochi → Onyx rename bridge | USR2 hot-upgrade must succeed from Orochi-branded predecessors into Onyx Server images (no cold restart / session drop) | **Server (HEAD `d158489`):** dual capability tokens — `ONYX_HELIX_UPGRADE_CAPS=…` + legacy `OROCHI_HELIX_UPGRADE_CAPS=…` with identical caps set (`helix/live.zig`); `hasUpgradeCapabilityLine` exact-matches either full line; `--helix-upgrade-capabilities-v1` prints both (`main.zig`); resume accepts `OROCHI_HELIX_*` env aliases then prefers `ONYX_HELIX_*`. New handoffs emit ONYX names only. **Not done:** remove legacy aliases (only after no Orochi predecessor remains); live unit/binary deploy still human-gated (`ROADMAP.md` engine-rename row). | closed 2026-07-19 (rename-window bridge); alias removal deferred |
| Era 1 / Era 2 client fleet wave (2026-07-19) | Thesis-visible client wave + server mesh hardening land together; ledger reflects shipped vs partial honestly | **Client `9cf2a50` + 2026-07-21 follow-ons:** NAMES mid-burst; B1 TOKEN/MTOKEN; B4 ACCESS; vault hydrate/time-travel; cold-return Home; A1 hybrid search (**closed**); **B2 R1–R6 closed** (`d415302`); **background consolidation closed** (`9629f2d`); **dense nicklist AppShell e2e closed** (`e65675a`); offline memos **MEMO-only** (`427209d`, no `TEGAMI` dual-accept). **Partial:** B3 deeper multiline send optional; **B8** list+DROP closed, recovery codes still open. **Server fleet:** mIRC admit, NAMES/membership/flood-guard, dual Helix brand tokens, fail-closed MESSAGE_V2 — `github/main` @ `54b9fb4`. **Deploy:** human-gated (not claimed live here). | partial 2026-07-21 — see per-slice rows; prod deploy not asserted |
| Era 2 B8 sessions list + DROP | Authoritative sessions/devices list and remote revoke | **List + remote DROP closed:** server `SESSION DROP #<n>`; client `sessionList.ts` + `SessionsDevicesSection` + store `refreshAccountSessions` / `dropAccountSession`; tests `sessionList.test.ts`, `SessionsDevicesSection.test.tsx` (`d08faf2`). **Recovery codes still open:** no client/server recovery-code surface under `src/` (only roadmap copy in `Roadmap.tsx`) | partial 2026-07-21 — list+DROP closed; recovery codes open |
| Offline memo wire (MEMO-only) | Client accepts offline memo delivery; historical `TEGAMI` dual-accept retired | Store folds `NOTE MEMO` only (`store.ts` `standard.command === 'MEMO'`); parser memo ceiling is MEMO-keyed; no `TEGAMI` token in `store.ts` / `parser.ts` (`427209d`); DM sidebar surfaces `offlineMemo` counts (`708a8c5`). Protocol note: `ONYX_SERVER_PROTOCOL.md` / `docs/README.md` say MEMO only | closed 2026-07-21 (MEMO-only; recovery codes unrelated) |
